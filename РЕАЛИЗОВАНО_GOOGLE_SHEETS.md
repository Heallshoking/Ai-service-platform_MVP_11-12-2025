# 📊 Google Sheets Integration - РЕАЛИЗОВАНО

## ✅ ЧТО СДЕЛАНО:

### 1. Google Sheets Integration Module (`google_sheets_integration.py`)

**416 строк кода** - полноценная интеграция с Google Sheets:

#### Основной класс: `GoogleSheetsManager`

**Методы:**
- ✅ `__init__()` - подключение к Google Sheets через Service Account
- ✅ `init_worksheets()` - автоматическое создание 3 листов
- ✅ `add_order(order_data)` - добавление новой заявки
- ✅ `get_orders(status)` - получение списка заявок с фильтром
- ✅ `assign_master(order_id, master_name)` - назначение мастера
- ✅ `complete_order(order_id, price, rating)` - завершение заявки с расчетом комиссии 30%
- ✅ `add_master(master_data)` - добавление мастера
- ✅ `get_masters(status)` - получение списка мастеров
- ✅ `update_stats()` - автоматическое обновление статистики

**Вспомогательные функции:**
- ✅ `save_order_from_bot()` - сохранение заявки из Telegram бота
- ✅ `save_order_from_website()` - сохранение заявки с сайта

#### Структура Google Sheets (3 листа):

**Лист 1: "Заявки" (15 колонок)**
```
1. ID - уникальный номер заявки
2. Дата/Время - когда создана
3. Источник - telegram / website
4. Имя - имя клиента
5. Телефон - телефон клиента
6. Категория - тип услуги
7. Проблема - описание проблемы
8. Адрес - адрес клиента
9. Статус - Новая / В работе / Выполнена
10. Мастер - кто назначен
11. Цена - стоимость работы
12. Комиссия 30% - автоматический расчет
13. Дата выполнения - когда завершена
14. Оценка - рейтинг 1-5
15. Комментарий - отзыв клиента
```

**Лист 2: "Мастера" (10 колонок)**
```
1. ID
2. Telegram ID
3. Имя
4. Телефон
5. Специализация
6. Рейтинг
7. Заказов выполнено
8. Заработано
9. Статус (Активен/Неактивен)
10. Дата регистрации
```

**Лист 3: "Статистика" (5 колонок)**
```
1. Метрика - название показателя
2. Значение - число
3. Период - временной отрезок
4. Обновлено - когда обновлено
5. Примечание - дополнительная информация
```

**Автоматические метрики:**
- Всего заявок
- Новых заявок (требуют назначения)
- В работе
- Выполнено
- Общая выручка
- Наша комиссия (30%)

---

### 2. Обновленный Telegram Client Bot (`telegram_client_bot.py`)

**Добавлена интеграция:**

```python
# Импорт Google Sheets с graceful fallback
try:
    from google_sheets_integration import save_order_from_bot
    GOOGLE_SHEETS_ENABLED = True
except:
    print("⚠️ Google Sheets не подключен, заявки будут только на API")
    GOOGLE_SHEETS_ENABLED = False
```

**Модифицирована функция `confirm()`:**

```python
# Сохранение заявки в Google Sheets
order_id = None
if GOOGLE_SHEETS_ENABLED:
    try:
        order_id = save_order_from_bot(
            name=data['name'],
            phone=data['phone'],
            category=data.get('category_name', data.get('category', '')),
            problem=data['problem'],
            address=data['address'],
            source="telegram"
        )
        if order_id:
            print(f"✅ Заявка #{order_id} сохранена в Google Sheets")
    except Exception as e:
        print(f"⚠️ Ошибка сохранения в Google Sheets: {e}")
```

**Обновленное сообщение клиенту:**

```
✅ Заявка создана!

🎫 Номер заявки: #123
📋 Отследить: https://app.balt-set.ru/track.html

📞 Мастер свяжется с вами в течение 15 минут!
```

**Преимущества:**
- ✅ Graceful degradation - бот работает даже без Google Sheets
- ✅ Клиент получает номер заявки для отслеживания
- ✅ Все данные сохраняются в таблице автоматически

---

### 3. Admin Dashboard (`static/dashboard.html`)

**536 строк кода** - современный дашборд с интеграцией Google Sheets:

#### Визуальные компоненты:

**1. Карточки статистики (4 штуки):**
- 📋 Всего заявок - общее количество
- 🆕 Новые заявки - требуют назначения мастера
- ⚙️ В работе - мастер уже назначен
- ✅ Выполнено - с расчетом комиссии 30%

**2. Быстрые действия (3 кнопки):**
- 📊 Открыть Google Sheets - прямая ссылка на таблицу
- 🔄 Обновить статистику - перезагрузка данных
- 🤖 Управление ботами - SSH команды

**3. Вкладки (3 листа):**
- 📋 Заявки - встроенный просмотр листа "Заявки"
- 👷 Мастера - встроенный просмотр листа "Мастера"
- 📊 Статистика - встроенный просмотр листа "Статистика"

**4. Встроенный просмотр Google Sheets:**
- Использует iframe с прямым доступом к таблице
- Возможность редактирования прямо в дашборде
- Автоматическая синхронизация изменений

#### Технические особенности:

**Дизайн:**
- Калининградский фирменный стиль (градиент #667eea → #764ba2)
- Адаптивная верстка (responsive)
- Современные карточки с hover-эффектами
- Красивая анимация загрузки

**Функциональность:**
- Переключение между листами без перезагрузки
- Автоматическое обновление статистики
- Прямая интеграция с Google Sheets
- Информационный блок о настройке

**Настройка:**
```javascript
// Вставьте сюда ID вашей Google Таблицы
const SPREADSHEET_ID = ''; // Например: '1BxiMVs0...'

// GID листов (из URL после #gid=)
const SHEET_GIDS = {
    orders: '0',   // Заявки
    masters: '1',  // Мастера  
    stats: '2'     // Статистика
};
```

---

### 4. Документация

#### `НАСТРОЙКА_GOOGLE_SHEETS.md` (288 строк)
- Полная инструкция по настройке Google Cloud
- Создание Service Account
- Настройка доступов
- Развертывание на VPS
- Решение проблем

#### `БЫСТРЫЙ_СТАРТ_GOOGLE_SHEETS.md` (252 строки)
- Пошаговая инструкция на 10 минут
- 8 простых шагов
- Команды для копирования
- Тестирование системы
- Troubleshooting

#### `СХЕМА_СИСТЕМЫ.md` (355 строк)
- Полная архитектура системы
- Схема интеграций
- Критические проблемы и решения

#### `УПРАВЛЕНИЕ_БОТАМИ.md` (276 строк)
- SSH команды для управления
- Просмотр логов
- Перезапуск ботов
- Мониторинг

---

### 5. Скрипты деплоя

#### `DEPLOY_GOOGLE_SHEETS.sh` (81 строка)
- Автоматическое копирование файлов на VPS
- Установка зависимостей
- Перезапуск бота
- Проверка статуса

#### `deploy_dashboard.sh` (21 строка)
- Загрузка dashboard.html на сайт
- Проверка успешности
- Инструкции по настройке

---

## 🎯 АРХИТЕКТУРА СИСТЕМЫ:

```
┌─────────────────┐
│  Telegram Bot   │
│   (Клиент)      │
└────────┬────────┘
         │
         ├─────────────────────────────┐
         │                             │
         ▼                             ▼
┌─────────────────┐          ┌─────────────────┐
│ Google Sheets   │          │   API (опц.)    │
│   Integration   │          │   (fallback)    │
└────────┬────────┘          └─────────────────┘
         │
         │ Автоматически создает:
         │
         ├──► "Заявки" (15 колонок)
         ├──► "Мастера" (10 колонок)
         └──► "Статистика" (5 колонок)
                      │
                      ▼
         ┌─────────────────────────┐
         │   Admin Dashboard       │
         │   (dashboard.html)      │
         │                         │
         │ - Карточки статистики   │
         │ - Встроенный просмотр   │
         │ - Быстрые действия      │
         └─────────────────────────┘
```

---

## 📊 DATA FLOW (Поток данных):

```
1. Клиент начинает диалог с ботом
   ↓
2. Бот собирает данные:
   - Категория → Проблема → Адрес → Имя → Телефон
   ↓
3. Клиент подтверждает заявку
   ↓
4. telegram_client_bot.py вызывает save_order_from_bot()
   ↓
5. google_sheets_integration.py:
   - Генерирует уникальный ID
   - Добавляет строку в лист "Заявки"
   - Обновляет лист "Статистика"
   ↓
6. Возвращает order_id боту
   ↓
7. Бот отправляет клиенту:
   "✅ Заявка создана! 🎫 Номер заявки: #123"
   ↓
8. Админ открывает dashboard.html
   ↓
9. Dashboard загружает данные из Google Sheets через iframe
   ↓
10. Админ видит все заявки, может редактировать, назначать мастеров
```

---

## 💰 КОМИССИЯ 30%:

**Автоматический расчет:**

```python
def complete_order(self, order_id: int, price: float, rating: int = 5):
    """Завершить заявку"""
    
    # Комиссия 30%
    commission = price * 0.30
    
    # Обновляем данные в Google Sheets
    self.orders_sheet.update_cell(row_num, 11, str(price))       # Цена
    self.orders_sheet.update_cell(row_num, 12, str(commission))  # Комиссия
    
    # Мастер получает: price - commission = 70%
```

**Пример:**
- Заказ на 10 000₽
- Комиссия платформы: 3 000₽ (30%)
- Мастер получает: 7 000₽ (70%)

**Важно:** Комиссия НЕ афишируется клиентам (как и требовалось)

---

## 🔐 БЕЗОПАСНОСТЬ:

**Service Account:**
- Доступ только через credentials.json
- Права только на чтение/запись в конкретную таблицу
- Нет доступа к другим Google аккаунтам

**VPS:**
- credentials.json хранится в /root/ai_service_bots/ (только root)
- Права на файл: 600 (только владелец)

**Dashboard:**
- Только для просмотра (не содержит секретов)
- Требуется SPREADSHEET_ID (настраивается вручную)

---

## 🚀 ДЕПЛОЙ:

### Вариант 1: Автоматический (expect)

```bash
chmod +x DEPLOY_GOOGLE_SHEETS.sh
./DEPLOY_GOOGLE_SHEETS.sh
```

### Вариант 2: Ручной (копирование файлов)

```bash
# 1. Копируем файлы
scp google_sheets_integration.py root@176.98.178.109:/root/ai_service_bots/
scp telegram_client_bot.py root@176.98.178.109:/root/ai_service_bots/
scp credentials.json root@176.98.178.109:/root/ai_service_bots/

# 2. Подключаемся к VPS
ssh root@176.98.178.109

# 3. Устанавливаем зависимости
pip3 install gspread oauth2client

# 4. Перезапускаем бота
cd /root/ai_service_bots/
pkill -f telegram_client_bot.py
sleep 2
nohup python3 telegram_client_bot.py > client_bot.log 2>&1 &

# 5. Проверяем
ps aux | grep telegram_client_bot.py | grep -v grep
tail -f client_bot.log
```

### Вариант 3: Dashboard

```bash
chmod +x deploy_dashboard.sh
./deploy_dashboard.sh
```

---

## 📈 СЛЕДУЮЩИЕ ШАГИ:

### Что нужно сделать ВАМ (10 минут):

1. ✅ Создать Google Cloud проект
2. ✅ Включить Google Sheets API и Google Drive API
3. ✅ Создать Service Account
4. ✅ Скачать credentials.json
5. ✅ Создать таблицу "BALT-SET Заявки"
6. ✅ Поделиться таблицей с Service Account email
7. ✅ Загрузить credentials.json на VPS
8. ✅ Вставить SPREADSHEET_ID в dashboard.html
9. ✅ Протестировать бота
10. ✅ Открыть dashboard

**Инструкция:** `БЫСТРЫЙ_СТАРТ_GOOGLE_SHEETS.md`

---

## 🎉 ИТОГ:

### Создано файлов: 6
1. ✅ `google_sheets_integration.py` - 416 строк
2. ✅ `telegram_client_bot.py` - обновлен
3. ✅ `static/dashboard.html` - 536 строк
4. ✅ `НАСТРОЙКА_GOOGLE_SHEETS.md` - 288 строк
5. ✅ `БЫСТРЫЙ_СТАРТ_GOOGLE_SHEETS.md` - 252 строки
6. ✅ `deploy_dashboard.sh` - 21 строка

### Создано скриптов: 1
1. ✅ `DEPLOY_GOOGLE_SHEETS.sh` - 81 строка

### Всего строк кода: ~1600+

### Функционал:
✅ Интеграция с Google Sheets  
✅ 3 автоматических листа  
✅ Автоматический расчет комиссии 30%  
✅ Админ-панель с встроенным просмотром  
✅ Graceful degradation (работает без Google Sheets)  
✅ Номера заявок для клиентов  
✅ Автоматическая статистика  
✅ Полная документация  

---

🎯 **Система готова к развертыванию!**

Следуйте инструкции `БЫСТРЫЙ_СТАРТ_GOOGLE_SHEETS.md` для финальной настройки.
