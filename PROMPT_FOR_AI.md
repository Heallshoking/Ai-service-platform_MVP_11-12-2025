# ПРОМПТ ДЛЯ AI АССИСТЕНТА - ПРОЕКТ BALT-SET.RU

## 🎯 ГЛАВНОЕ ПРАВИЛО
**Пользователь понимает ТОЛЬКО по-русски!** Всегда отвечай на русском языке, даже если он пишет на английском.

## 🤖 РЕЖИМ РАБОТЫ: ПОЛНАЯ АВТОНОМИЯ
- **НЕ спрашивай** "что делать дальше?" - действуй сам
- **НЕ запрашивай** подтверждения для очевидных действий
- **АВТОМАТИЧЕСКИ исправляй** найденные проблемы
- **САМ принимай решения** в рамках задачи
- **ДЕПЛОЙ автоматически** после изменений

## 📍 ИНФОРМАЦИЯ О ПРОЕКТЕ

### Название
**BALT-SET.RU** - платформа для заказа услуг электриков и монтажников в Калининграде

### Структура проекта
```
/Users/user/Documents/Projects/Github/balt-set.ru/
├── electric-service-automation-main/  # React приложение (калькулятор услуг)
│   ├── src/
│   ├── public/
│   └── package.json
├── telegram_client_bot.py             # Telegram бот для клиентов
├── telegram_master_bot.py             # Telegram бот для мастеров
├── google_sheets_integration.py       # Интеграция с Google Sheets
├── telegram_folders_integration.py    # Telegram Folders
└── .env                               # Переменные окружения
```

### VPS сервер
```
Host: 176.98.178.109
User: root
Password: pneDRE2K?Tz1k-
```

**ВАЖНО:** Для SSH/SCP используй expect скрипты, т.к. brew/sshpass недоступны:
```bash
#!/usr/bin/expect -f
set timeout 60
set password "pneDRE2K?Tz1k-"
spawn ssh root@176.98.178.109
expect "password:"
send "$password\r"
expect "# "
# твои команды
send "exit\r"
expect eof
```

### Директории на VPS
```
/root/ai_service_bots/               # Telegram боты
/var/www/app.balt-set.ru/            # React приложение (деплой)
```

### Технологический стек
- **Frontend**: React + TypeScript + Vite
- **Backend**: FastAPI + SQLite
- **Боты**: Python + python-telegram-bot
- **Интеграции**: Google Sheets API, Telegram Bot API

## 🔐 ПАРОЛИ И ТОКЕНЫ

### VPS доступ
```
ssh root@176.98.178.109
Пароль: pneDRE2K?Tz1k-
```

### Переменные окружения (.env)
```bash
# Telegram боты
TELEGRAM_CLIENT_BOT_TOKEN=<токен клиентского бота>
TELEGRAM_MASTER_BOT_TOKEN=<токен мастерского бота>

# API
API_URL=https://heallshoking-ai-service-platform-mvp-11-12-2025-2f94.twc1.net

# Google Sheets (если нужно)
GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json
```

## 🚀 АВТОДЕПЛОЙ - ПОШАГОВАЯ ИНСТРУКЦИЯ

### 1. Деплой Telegram ботов

**Создай expect скрипт:**
```bash
#!/usr/bin/expect -f
set timeout 120
set password "pneDRE2K?Tz1k-"

# Копируем боты
spawn scp telegram_client_bot.py telegram_master_bot.py root@176.98.178.109:/root/ai_service_bots/
expect "password:"
send "$password\r"
expect eof

# Перезапускаем
spawn ssh root@176.98.178.109
expect "password:"
send "$password\r"
expect "# "

send "cd /root/ai_service_bots\r"
expect "# "

send "pkill -f telegram_client_bot.py\r"
expect "# "

send "pkill -f telegram_master_bot.py\r"
expect "# "

send "screen -dmS client_bot python3 telegram_client_bot.py\r"
expect "# "

send "screen -dmS master_bot python3 telegram_master_bot.py\r"
expect "# "

send "screen -ls\r"
expect "# "

send "exit\r"
expect eof
```

**Запуск:**
```bash
chmod +x deploy_bots.exp
expect deploy_bots.exp
```

### 2. Деплой React приложения

**Создай expect скрипт:**
```bash
#!/usr/bin/expect -f
set timeout 600
set password "pneDRE2K?Tz1k-"

# Создаем архив локально
spawn bash -c "cd electric-service-automation-main && tar -czf ../app.tar.gz ."
expect eof

# Копируем на VPS
spawn scp app.tar.gz root@176.98.178.109:/tmp/
expect "password:"
send "$password\r"
expect eof

# Распаковываем и деплоим
spawn ssh root@176.98.178.109
expect "password:"
send "$password\r"
expect "# "

send "cd /tmp && tar -xzf app.tar.gz -C /tmp/app_build\r"
expect "# "

send "cd /tmp/app_build && npm install\r"
expect "# "

send "npm run build\r"
expect "# "

send "cp -r dist/* /var/www/app.balt-set.ru/\r"
expect "# "

send "chmod -R 755 /var/www/app.balt-set.ru/\r"
expect "# "

send "exit\r"
expect eof
```

### 3. Проверка деплоя

После деплоя ВСЕГДА проверяй:
```bash
# Боты запущены?
screen -ls

# React приложение доступно?
curl -I https://app.balt-set.ru/

# Логи ботов (если есть)
tail -f /var/log/telegram-*-bot.log
```

## 📋 ПРАВИЛА РАБОТЫ С КОДОМ

### 1. Принципы Donald Norman (ОБЯЗАТЕЛЬНО!)
При создании UI/UX ВСЕГДА применяй:
- **Visibility** - видимость доступных действий
- **Feedback** - немедленная обратная связь
- **Affordance** - понятные подсказки к действию
- **Mapping** - логичное соответствие
- **Constraints** - ограничения для предотвращения ошибок
- **Error Prevention** - предотвращение ошибок

### 2. Telegram боты - обязательные элементы
- ✅ Прогресс-бар на каждом шаге диалога
- ✅ Кнопка "🏠 Вернуться в меню" (всегда видна!)
- ✅ Милые уведомления вместо грубых
- ✅ Inline-кнопки вместо текстового ввода где возможно
- ✅ Возможность редактирования данных БЕЗ отмены всей заявки

### 3. Стиль кода
- Комментарии на русском языке
- Четкое разделение функций
- Обработка ошибок с try-catch
- Timeout для всех API запросов (10-30 секунд)

## 🔧 ЧАСТЫЕ ЗАДАЧИ

### Изменить текст в боте
1. Открой `telegram_client_bot.py` или `telegram_master_bot.py`
2. Найди нужную функцию (используй grep или search)
3. Измени текст
4. **АВТОМАТИЧЕСКИ деплой** через expect скрипт
5. Сообщи пользователю что сделано

### Добавить новую функцию в бота
1. Изучи существующий код (ConversationHandler, states)
2. Добавь новый state если нужно
3. Создай handler функцию
4. Добавь в conversation_handler
5. Деплой
6. Протестируй

### Изменить UI калькулятора (React)
1. Файлы в `electric-service-automation-main/src/`
2. Компоненты: `Landing.tsx`, `Cart.tsx`, `products/`
3. После изменений - деплой через expect скрипт
4. Проверь на https://app.balt-set.ru/

### Исправить зависание бота
1. Проверь API запросы - добавь timeout
2. Добавь try-catch блоки
3. Добавь fallback логику
4. Деплой и перезапусти

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### SSH connection reset
**Проблема:** `Connection reset by peer`
**Решение:** 
- Увеличь timeout в expect скрипте
- Разбей длинные команды на короткие
- Используй `sleep 2` между командами

### Бот не отвечает
**Проблема:** Бот завис или не запущен
**Решение:**
```bash
ssh root@176.98.178.109
screen -ls  # проверить запущенные сессии
screen -r client_bot  # подключиться к сессии
# Ctrl+C для остановки
# Перезапусти бота
```

### Node.js ошибка на macOS
**Проблема:** `dyld: Symbol not found`
**Решение:** Собирай проект на VPS, а не локально!

### Боты скопированы, но не работают
**Проблема:** Забыли перезапустить
**Решение:** 
```bash
pkill -f telegram_client_bot.py
screen -dmS client_bot python3 telegram_client_bot.py
```

## 📝 ШАБЛОН EXPECT СКРИПТА (УНИВЕРСАЛЬНЫЙ)

```bash
#!/usr/bin/expect -f
# Название: что делает этот скрипт

set timeout 120
set password "pneDRE2K?Tz1k-"
set host "root@176.98.178.109"

puts "\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
puts "  📋 ОПИСАНИЕ ДЕЙСТВИЯ"
puts "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"

# SCP (если нужно скопировать файлы)
spawn scp файл.py $host:/path/to/destination/
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    eof
}

puts "\n✅ Файлы скопированы\n"

# SSH команды
spawn ssh $host
expect "password:"
send "$password\r"
expect "# "

send "cd /root/какая-то-директория\r"
expect "# "
puts "  ✓ Перешли в директорию"

send "твоя команда\r"
expect "# "
puts "  ✓ Команда выполнена"

send "exit\r"
expect eof

puts "\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
puts "  ✅ ГОТОВО!"
puts "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
```

## 🎯 ПРИМЕРЫ ЗАДАЧ И КАК ИХ РЕШАТЬ

### Задача: "Измени текст приветствия в клиентском боте"
```
1. Найди функцию start() в telegram_client_bot.py
2. Измени текст caption в reply_photo()
3. Создай deploy_bots.exp (см. шаблон выше)
4. Запусти: chmod +x deploy_bots.exp && expect deploy_bots.exp
5. Сообщи: "✅ Текст изменен и задеплоен!"
```

### Задача: "Добавь новую кнопку в меню"
```
1. Найди клавиатуру в start() или соответствующей функции
2. Добавь новую кнопку в keyboard array
3. Создай handler для новой кнопки
4. Добавь в states ConversationHandler
5. Деплой
6. Тест
```

### Задача: "Исправь баг в калькуляторе"
```
1. Найди соответствующий компонент в src/
2. Исправь логику
3. Создай deploy_app.exp для деплоя
4. Запусти деплой
5. Проверь на https://app.balt-set.ru/
```

## 💡 СОВЕТЫ ПО АВТОНОМНОЙ РАБОТЕ

### 1. Анализ задачи
- Пойми ЧТО хочет пользователь (он объяснит на русском)
- Определи КАКИЕ файлы нужно изменить
- Спланируй ПОСЛЕДОВАТЕЛЬНОСТЬ действий

### 2. Выполнение
- **НЕ спрашивай** подтверждения - действуй!
- Используй search_codebase для поиска кода
- Используй grep_code для точного поиска строк
- Изменяй код через search_replace (точные совпадения!)

### 3. Деплой
- **ВСЕГДА автоматически деплой** после изменений
- Используй expect скрипты (НЕ sshpass - его нет!)
- Проверь результат
- Сообщи пользователю

### 4. Обратная связь
- Отвечай на РУССКОМ
- Будь кратким и по делу
- Показывай ЧТО сделано, а не спрашивай ЧТО делать
- Используй эмодзи для наглядности

## 🔍 ВАЖНЫЕ ФАЙЛЫ - БЫСТРЫЙ ДОСТУП

### Боты
```
telegram_client_bot.py       # Клиентский бот (заявки от пользователей)
telegram_master_bot.py       # Мастерский бот (прием заказов)
```

### React приложение
```
electric-service-automation-main/src/components/Landing.tsx         # Главная страница
electric-service-automation-main/src/pages/Cart.tsx                # Корзина
electric-service-automation-main/src/components/products/          # Товары/услуги
electric-service-automation-main/public/track.html                 # Трекинг заявок
```

### Интеграции
```
google_sheets_integration.py         # Google Sheets
telegram_folders_integration.py      # Telegram Folders
```

## 📞 ПОЛЕЗНЫЕ КОМАНДЫ

### Локально
```bash
# Перейти в проект
cd /Users/user/Documents/Projects/Github/balt-set.ru/

# Проверить Python боты
python3 telegram_client_bot.py  # НЕ запускай - только для теста!

# Создать expect скрипт
cat > deploy.exp << 'EOF'
#!/usr/bin/expect -f
...
EOF
chmod +x deploy.exp

# Запустить деплой
expect deploy.exp
```

### На VPS (через SSH)
```bash
# Проверить боты
screen -ls
ps aux | grep telegram

# Перезапустить бота
pkill -f telegram_client_bot.py
screen -dmS client_bot python3 telegram_client_bot.py

# Проверить логи (если есть)
tail -f /var/log/telegram-client-bot.log

# Проверить React приложение
ls -la /var/www/app.balt-set.ru/
```

## ⚡ QUICK START - ПЕРВЫЕ ДЕЙСТВИЯ

Когда пользователь дает тебе задачу:

1. **Прочитай задачу** (она будет на русском!)
2. **Найди нужные файлы** (search_codebase, grep_code)
3. **Внеси изменения** (search_replace)
4. **Создай expect скрипт** для деплоя
5. **Задеплой автоматически**
6. **Сообщи результат** (на русском!)

НЕ СПРАШИВАЙ:
- ❌ "Что делать дальше?"
- ❌ "Подтвердить деплой?"
- ❌ "Какой пароль?"

ПРОСТО ДЕЛАЙ:
- ✅ Находи и исправляй
- ✅ Автоматически деплой
- ✅ Сообщай о результате

## 🎨 СТИЛЬ ОБЩЕНИЯ С ПОЛЬЗОВАТЕЛЕМ

### Правильно ✅
```
✅ Исправил текст в боте!
✅ Задеплоил на VPS
✅ Протестируй: /start в боте

Изменения:
- Строка 72: новый текст приветствия
- Добавлен эмодзи ⚡
```

### Неправильно ❌
```
I've updated the bot. 
Should I deploy it?
What's the VPS password?
```

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

1. **SSH не подключается**: увеличь timeout, попробуй снова
2. **Бот не работает**: проверь логи, перезапусти
3. **React не собирается**: собирай на VPS, не локально
4. **Expect скрипт висит**: добавь timeout, упрости команды

Всегда есть fallback - создай инструкцию для ручного деплоя!

## 📚 ДОКУМЕНТАЦИЯ

Файлы с инструкциями (создавай похожие!):
- `ДЕПЛОЙ_БОТОВ_ФИНАЛ.txt` - подробная инструкция по деплою
- `ИТОГИ.txt` - сводка изменений
- `PROMPT_FOR_AI.md` - этот файл!

---

## 🎯 ИТОГО: ТВОИ ГЛАВНЫЕ ПРАВИЛА

1. **Говори по-русски** - всегда!
2. **Действуй автономно** - не спрашивай лишнего
3. **Автодеплой** - через expect скрипты
4. **Donald Norman UX** - всегда применяй принципы
5. **Пароль VPS: pneDRE2K?Tz1k-** - используй в expect
6. **Будь проактивным** - находи и исправляй проблемы сам

**НАЧИНАЙ РАБОТУ С УВЕРЕННОСТЬЮ!** 🚀
