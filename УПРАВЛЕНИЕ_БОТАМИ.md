# 🎛️ УПРАВЛЕНИЕ БОТАМИ НА VPS

## 📍 **РАСПОЛОЖЕНИЕ ФАЙЛОВ НА VPS:**

### Сервер:
```
VPS: 176.98.178.109
Пользователь: root
Пароль: pneDRE2K?Tz1k-
```

### Директория ботов:
```bash
/root/ai_service_bots/
```

### Файлы:
```
/root/ai_service_bots/
├── telegram_client_bot.py    # Бот для клиентов
├── telegram_master_bot.py    # Бот для мастеров
├── .env                       # Токены и настройки
├── client.log                 # Логи бота клиента
└── master.log                 # Логи бота мастера
```

---

## 🔍 **ПРОВЕРКА СТАТУСА БОТОВ:**

### Подключиться к VPS:
```bash
ssh root@176.98.178.109
# Пароль: pneDRE2K?Tz1k-
```

### Проверить работающие процессы:
```bash
ps aux | grep telegram
```

Должны быть:
```
python3 telegram_client_bot.py
python3 telegram_master_bot.py
```

### Проверить PID:
```bash
pgrep -f telegram_client_bot.py
pgrep -f telegram_master_bot.py
```

---

## 📊 **ПРОСМОТР ЛОГОВ:**

### Логи бота клиента (последние 30 строк):
```bash
ssh root@176.98.178.109 "tail -30 /root/ai_service_bots/client.log"
```

### Логи бота мастера (последние 30 строк):
```bash
ssh root@176.98.178.109 "tail -30 /root/ai_service_bots/master.log"
```

### Следить за логами в реальном времени:
```bash
ssh root@176.98.178.109
cd /root/ai_service_bots

# Бот клиента
tail -f client.log

# Бот мастера
tail -f master.log
```

### Посмотреть ошибки:
```bash
ssh root@176.98.178.109 "grep -i error /root/ai_service_bots/client.log | tail -20"
ssh root@176.98.178.109 "grep -i error /root/ai_service_bots/master.log | tail -20"
```

---

## 🔄 **ПЕРЕЗАПУСК БОТОВ:**

### Перезапустить оба бота:
```bash
ssh root@176.98.178.109 << 'EOF'
cd /root/ai_service_bots

# Остановка
pkill -9 -f telegram_client_bot.py
pkill -9 -f telegram_master_bot.py
sleep 2

# Запуск
nohup python3 telegram_client_bot.py > client.log 2>&1 &
nohup python3 telegram_master_bot.py > master.log 2>&1 &
sleep 3

# Проверка
ps aux | grep telegram
EOF
```

### Перезапустить только бота клиента:
```bash
ssh root@176.98.178.109 << 'EOF'
cd /root/ai_service_bots
pkill -9 -f telegram_client_bot.py
sleep 2
nohup python3 telegram_client_bot.py > client.log 2>&1 &
ps aux | grep telegram_client
EOF
```

### Перезапустить только бота мастера:
```bash
ssh root@176.98.178.109 << 'EOF'
cd /root/ai_service_bots
pkill -9 -f telegram_master_bot.py
sleep 2
nohup python3 telegram_master_bot.py > master.log 2>&1 &
ps aux | grep telegram_master
EOF
```

---

## 🛑 **ОСТАНОВКА БОТОВ:**

### Остановить оба бота:
```bash
ssh root@176.98.178.109 "pkill -9 -f telegram_client_bot.py; pkill -9 -f telegram_master_bot.py"
```

### Остановить только бота клиента:
```bash
ssh root@176.98.178.109 "pkill -9 -f telegram_client_bot.py"
```

### Остановить только бота мастера:
```bash
ssh root@176.98.178.109 "pkill -9 -f telegram_master_bot.py"
```

---

## 🚀 **ДЕПЛОЙ ОБНОВЛЕНИЙ (АВТОМАТИЧЕСКИЙ):**

### С вашего Mac запустите:
```bash
cd /Users/user/Documents/Projects/Github/balt-set.ru
expect FINAL_DEPLOY.sh
```

Скрипт автоматически:
1. ✅ Скопирует обновлённые боты на VPS
2. ✅ Остановит старые процессы
3. ✅ Запустит новые
4. ✅ Покажет статус

---

## 📝 **РЕДАКТИРОВАНИЕ ФАЙЛОВ НА VPS:**

### Открыть файл для редактирования:
```bash
ssh root@176.98.178.109
cd /root/ai_service_bots

# Редактировать бота клиента
nano telegram_client_bot.py

# Редактировать бота мастера
nano telegram_master_bot.py

# Редактировать настройки
nano .env
```

После редактирования:
```
Ctrl + O  # Сохранить
Enter     # Подтвердить
Ctrl + X  # Выйти
```

---

## 🔧 **ПРОВЕРКА БОТА МАСТЕРА:**

### Проверить работает ли:
```bash
ssh root@176.98.178.109 "pgrep -f telegram_master_bot.py"
```

Если **НЕТ** вывода → бот НЕ работает

### Посмотреть что случилось:
```bash
ssh root@176.98.178.109 "tail -50 /root/ai_service_bots/master.log"
```

### Перезапустить с выводом ошибок:
```bash
ssh root@176.98.178.109 << 'EOF'
cd /root/ai_service_bots
pkill -9 -f telegram_master_bot.py
sleep 2
python3 telegram_master_bot.py
EOF
```

Это запустит бота БЕЗ `nohup` - вы увидите все ошибки в реальном времени.

Для остановки: `Ctrl + C`

---

## 📱 **ТЕСТИРОВАНИЕ БОТОВ:**

### Бот клиента:
```
Telegram: @ai_service_client_bot
Команда: /start
```

### Бот мастера:
```
Telegram: @ai_service_master_bot
Команда: /start
```

---

## 💡 **БЫСТРЫЕ КОМАНДЫ:**

### Один блок для всего (скопируйте целиком):

#### Проверить статус:
```bash
ssh root@176.98.178.109 "cd /root/ai_service_bots && ps aux | grep telegram && echo '---CLIENT LOG---' && tail -5 client.log && echo '---MASTER LOG---' && tail -5 master.log"
```

#### Перезапустить всё:
```bash
ssh root@176.98.178.109 "cd /root/ai_service_bots && pkill -9 -f telegram && sleep 2 && nohup python3 telegram_client_bot.py > client.log 2>&1 & nohup python3 telegram_master_bot.py > master.log 2>&1 & sleep 3 && ps aux | grep telegram"
```

---

## 📞 **КОГДА МНЕ НУЖНА ПОМОЩЬ:**

Я уведомлю вас когда потребуется ввести пароль в терминале:

```
⚠️  ТРЕБУЕТСЯ ВАШЕ ДЕЙСТВИЕ: Введите пароль для SSH
```

Тогда вы вводите: `pneDRE2K?Tz1k-`

---

## ✅ **ТЕКУЩИЙ СТАТУС:**

- **Клиент:** @ai_service_client_bot
- **Мастер:** @ai_service_master_bot
- **VPS:** 176.98.178.109
- **Путь:** /root/ai_service_bots/
- **Комиссия:** 30%
