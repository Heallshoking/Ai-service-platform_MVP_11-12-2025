# Настройка системы винилового маркетплейса

## Обзор архитектуры

Система состоит из трёх компонентов:
- **Google Sheets** — единый источник правды (SSOT) для каталога пластинок
- **FastAPI Backend** — REST API для доступа к каталогу и генерации статических файлов
- **Telegram Bot** — админ-панель для управления каталогом
- **Frontend (React/Vue)** — потребляет API и статические JSON-файлы

## 1. Настройка Google Sheets

### Структура листа "Справочник"

Создайте таблицу с 21 колонкой (A-U):

| Колонка | Название | Тип | Описание |
|---------|----------|-----|----------|
| A | Артикул | Текст | VIN-00001 (генерируется автоматически) |
| B | Название | Текст | Название альбома |
| C | Исполнитель | Текст | Имя исполнителя/группы |
| D | Жанр | Текст | Музыкальный жанр |
| E | Год | Число | Год выпуска |
| F | Лейбл | Текст | Название лейбла |
| G | Страна | Текст | Страна производства |
| H | Формат | Текст | LP/EP/Single/Compilation |
| I | Состояние | Текст | Mint/Near Mint/VG+/VG/G+ |
| J | Цена | Число | Цена в рублях |
| K | ФОТО_URL | URL | Ссылка на фото (Google Drive) |
| L | Продавец_TG_ID | Число | Telegram ID продавца |
| M | Статус | Текст | 🟢 Доступна / 🟡 Зарезервирована / 🔴 Продана |
| N | Описание | Текст | AI-генерируемое описание |
| O | SEO_Title | Текст | SEO-заголовок для фронтенда |
| P | SEO_Description | Текст | SEO-описание для фронтенда |
| Q | Stock_Count | Число | Количество на складе |
| R | Минимум_складчиков | Число | Минимум участников складчины |
| S | Складчина_участников | Число | Текущее количество участников |
| T | Цена_ориентир | Число | Ориентировочная рыночная цена |
| U | Последний_интерес | Дата | Дата последнего интереса к пластинке |

**Заголовки в первой строке обязательны!**

### Дополнительные листы

Создайте следующие листы (при первом запуске бота они создадутся автоматически):
- **Пользователи** — регистрация пользователей бота
- **Предзаказы** — заявки на поиск/предзаказы
- **Фото_хеши** — дедупликация фотографий
- **Отчёты** — журнал действий в системе
- **Админ_события** — журнал админских действий
- **Избранное** — пластинки, за которыми следят пользователи

## 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Google Sheets API
GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_TELEGRAM_ID=your_telegram_user_id

# FastAPI
API_HOST=localhost
API_PORT=8000

# OpenAI (опционально для AI-описаний)
OPENAI_API_KEY=your_openai_key_here
```

### Получение учётных данных Google

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите Google Sheets API и Google Drive API
4. Создайте Service Account
5. Скачайте JSON с учётными данными
6. Укажите путь к файлу в `GOOGLE_SHEETS_CREDENTIALS_PATH`
7. Предоставьте Service Account доступ к вашей Google Sheets таблице (Editor)

### Получение Telegram Bot Token

1. Напишите [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте полученный токен в `TELEGRAM_BOT_TOKEN`
4. Получите свой Telegram ID (через [@userinfobot](https://t.me/userinfobot)) и укажите в `ADMIN_TELEGRAM_ID`

## 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

## 4. Запуск системы

### Вариант 1: Всё вместе (для разработки)

```bash
# Терминал 1: Запуск FastAPI
python main.py

# Терминал 2: Запуск Telegram бота
python vinyl_bot.py
```

### Вариант 2: Production (с использованием systemd)

Создайте unit-файлы для systemd:

**`/etc/systemd/system/vinyl-api.service`:**
```ini
[Unit]
Description=Vinyl Marketplace API
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/vinil_bot_kld
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

**`/etc/systemd/system/vinyl-bot.service`:**
```ini
[Unit]
Description=Vinyl Marketplace Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/vinil_bot_kld
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python vinyl_bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable vinyl-api vinyl-bot
sudo systemctl start vinyl-api vinyl-bot
```

## 5. Функции системы

### Для администратора (через Telegram бота):

1. **Добавление пластинки**: `/add_record` или кнопка "➕ ДОБАВИТЬ ПЛАСТИНКУ"
   - Автогенерация артикула VIN-XXXXX
   - AI-описание (если настроен OpenAI API)
   - Защита от дубликатов фото

2. **Поиск по артикулу**: Отправьте артикул (VIN-00001 или просто 1)
   
3. **Редактирование карточки** (кнопки в карточке пластинки):
   - ✏️ Изменить цену
   - 🔄 Изменить статус (Доступна/Резерв/Продана)
   - 📦 Обновить остаток (при stock=0 статус меняется на "Продана" автоматически)
   - 🌐 Обновить сайт (генерация статических JSON-файлов)

### Для покупателей (через Telegram бота):

- 🛒 КУПИТЬ — поиск по названию/исполнителю
- 💼 ПРОДАТЬ — режим продажи
- Складчины — групповые покупки редких пластинок
- Предзаказы — заявки на поиск редких изданий

### API эндпоинты:

- `GET /api/records` — получение каталога с фильтрацией
- `POST /api/generate-description` — генерация AI-описания
- `POST /admin/export-static` — генерация статических JSON

## 6. Генерация статического каталога

Экспорт производится в директорию `./static_export/`:

```
static_export/
├── catalog.json              # Полный каталог
└── products/
    ├── VIN-00001.json       # Индивидуальные карточки
    ├── VIN-00002.json
    └── ...
```

### Формат catalog.json:
```json
{
  "generated_at": "2024-12-08T12:34:56",
  "total_records": 150,
  "records": [...]
}
```

### Формат VIN-XXXXX.json:
```json
{
  "article_id": "VIN-00001",
  "title": "The Dark Side of the Moon",
  "artist": "Pink Floyd",
  "genre": "Progressive Rock",
  "year": 1973,
  "format": "LP",
  "condition": "Near Mint",
  "price": 5000,
  "stock_count": 1,
  "seo_title": "...",
  "seo_description": "..."
}
```

## 7. Интеграция с фронтендом

### Вариант 1: REST API (динамический контент)

```javascript
const response = await fetch('http://localhost:8000/api/records?limit=10&genre=Rock');
const data = await response.json();
```

### Вариант 2: Статические JSON (для SSG на Vercel)

```javascript
// На Vercel при билде
const catalog = await import('./static_export/catalog.json');
const product = await import(`./static_export/products/${article_id}.json`);
```

## 8. Устранение неполадок

### Бот не отвечает
- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь, что другой процесс не использует тот же токен
- Проверьте логи: `journalctl -u vinyl-bot -f`

### API возвращает 500
- Проверьте доступ к Google Sheets
- Проверьте credentials.json
- Логи: `journalctl -u vinyl-api -f`

### Не создаются артикулы
- Проверьте, что первая строка содержит заголовки
- Убедитесь, что колонка A называется "Артикул"

### Статический экспорт не работает
- Убедитесь, что API запущен
- Проверьте права на запись в `./static_export/`
- Проверьте endpoint: `curl -X POST http://localhost:8000/admin/export-static`

## 9. Масштабирование

- **Для большого каталога (>1000 записей)**: рассмотрите миграцию на PostgreSQL
- **Для высокой нагрузки**: добавьте Redis для кеширования
- **Для автообновления сайта**: настройте cron/APScheduler для периодической генерации

## 10. Безопасность

- ✅ Проверка ADMIN_TELEGRAM_ID для админских действий
- ⚠️ Для production: используйте HTTPS и реверс-прокси (nginx)
- ⚠️ Ограничьте доступ к `/admin/*` эндпоинтам через API keys
