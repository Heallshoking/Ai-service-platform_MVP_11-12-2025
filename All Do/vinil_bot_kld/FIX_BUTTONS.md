# 🔧 Исправление кнопок бота

## Проблема
На сервере запущена **старая версия** бота со старыми кнопками вместо новых.

## ✅ Решение (выполни на сервере)

### Вариант 1: Через SSH (рекомендуется)

```bash
# 1. Подключись к серверу
ssh root@176.98.178.109
# Пароль: b4aosWDARJY,rE

# 2. Перейди в директорию
cd /root/vinyl_marketplace_RU

# 3. Создай резервную копию старого бота
cp vinyl_bot.py vinyl_bot.py.backup

# 4. Загрузи новую версию (в другом терминале на Mac)
# Открой НОВЫЙ терминал на своём Mac и выполни:
cd /Users/user/Documents/Проекты/vinil_bot_kld
scp vinyl_bot.py root@176.98.178.109:/root/vinyl_marketplace_RU/

# 5. Вернись в SSH и перезапусти бота
pkill -f "python.*vinyl_bot.py"
nohup python3 vinyl_bot.py > /tmp/vinyl_bot.log 2>&1 &

# 6. Проверь, что запустился
ps aux | grep vinyl_bot
tail -f /tmp/vinyl_bot.log
```

### Вариант 2: Автоматический скрипт (с Mac)

```bash
# На своём Mac выполни:
cd /Users/user/Documents/Проекты/vinil_bot_kld
./quick_deploy_bot.sh
```

Введёшь пароль 2 раза, и всё обновится автоматически.

---

## 🎯 Проверка результата

После обновления в Telegram:
1. Отправь боту `/start`
2. Должны появиться кнопки:
   - 🛒 КУПИТЬ
   - 💼 ПРОДАТЬ
   - ➕ ДОБАВИТЬ ПЛАСТИНКУ

**Старые кнопки должны исчезнуть:**
- ❌ Каталог
- ❌ Мои пластинки  
- ❌ Настройки

---

## 📋 Если что-то пошло не так

### Бот не запускается

```bash
# Смотри логи
tail -100 /tmp/vinyl_bot.log

# Частые причины:
# 1. Нет .env файла
cat /root/vinyl_marketplace_RU/.env

# 2. Не хватает зависимостей
pip3 install python-telegram-bot --upgrade

# 3. Порт занят
pkill -f python
```

### Кнопки всё равно старые

```bash
# Убедись, что загрузился новый файл
cd /root/vinyl_marketplace_RU
ls -lah vinyl_bot.py

# Дата должна быть свежая (сегодня)
# Если старая - значит файл не загрузился

# Проверь размер файла
du -h vinyl_bot.py
# Должно быть ~75KB (новая версия)
```

### Бот работает, но старые кнопки

Это может быть **кэш Telegram**. Попробуй:
1. Удали чат с ботом
2. Заново найди бота
3. Отправь `/start`

---

## 🚀 Быстрое решение одной командой

На сервере:

```bash
ssh root@176.98.178.109 'cd /root/vinyl_marketplace_RU && pkill -f vinyl_bot && nohup python3 vinyl_bot.py > /tmp/vinyl_bot.log 2>&1 &'
```

Но сначала нужно загрузить новый файл!

---

## ❓ Вопросы?

Если проблема не решилась:
1. Покажи вывод: `cat /tmp/vinyl_bot.log | tail -50`
2. Покажи размер: `ls -lh /root/vinyl_marketplace_RU/vinyl_bot.py`
3. Дату файла: `stat /root/vinyl_marketplace_RU/vinyl_bot.py`
