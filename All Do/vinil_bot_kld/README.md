# 🎵 Граммофон — Винил Маркетплейс

Полнофункциональная система управления каталогом виниловых пластинок с Telegram-ботом для админов и REST API для фронтенда.

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонирование репозитория (если ещё не клонирован)
git clone <your-repo-url>
cd vinil_bot_kld

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` в корне проекта:

```bash
# Google Sheets
GOOGLE_SHEETS_CREDENTIALS_PATH=/path/to/credentials.json
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
ADMIN_TELEGRAM_ID=your_telegram_user_id

# API
API_HOST=localhost
API_PORT=8000

# OpenAI (опционально)
OPENAI_API_KEY=your_openai_key
```

### 3. Миграция Google Sheets

Запустите скрипт миграции для настройки структуры из 21 колонки:

```bash
python migrate_sheets.py
```

Скрипт автоматически:
- Создаст правильную структуру заголовков
- Сгенерирует артикулы VIN-XXXXX для существующих записей
- Установит значения по умолчанию для новых полей

### 4. Запуск

```bash
# Терминал 1: FastAPI Backend
python main.py

# Терминал 2: Telegram Bot
python vinyl_bot.py
```

## 📋 Основные возможности

### Для администратора (Telegram бот)

- ✅ **Добавление пластинок** — с автогенерацией артикула VIN-XXXXX
- 🔍 **Поиск по артикулу** — отправьте VIN-00001 или просто 1
- ✏️ **Редактирование цены** — через кнопку в карточке
- 🔄 **Изменение статуса** — Доступна/Резерв/Продана
- 📦 **Управление остатками** — с автообновлением статуса
- 🌐 **Экспорт сайта** — генерация статических JSON для фронтенда
- 🤖 **AI-описания** — автогенерация SEO-контента (требует OpenAI API)
- 🛡️ **Защита от дубликатов** — проверка хешей фотографий

### Для покупателей (Telegram бот)

- 🛒 **Поиск пластинок** — по названию, исполнителю, артикулу
- 🤝 **Складчины** — групповые покупки редких пластинок
- 📦 **Предзаказы** — заявки на поиск редких изданий
- 🔔 **Подписки** — уведомления о появлении нужных пластинок
- 📖 **История** — просмотр истории пластинок и описаний

### API для фронтенда

```bash
# Получить каталог
GET /api/records?limit=10&genre=Rock&status=available

# Экспорт статических файлов
POST /admin/export-static?output_dir=./static_export

# Генерация AI-описания
POST /api/generate-description
```

## 🗂️ Структура данных

### Лист "Справочник" (21 колонка)

| Колонка | Поле | Описание |
|---------|------|----------|
| A | Артикул | VIN-00001 (автогенерация) |
| B | Название | Название альбома |
| C | Исполнитель | Имя исполнителя |
| D | Жанр | Музыкальный жанр |
| E | Год | Год выпуска |
| F | Лейбл | Название лейбла |
| G | Страна | Страна производства |
| H | Формат | LP/EP/Single |
| I | Состояние | Mint/Near Mint/VG+ |
| J | Цена | Цена в рублях |
| K | ФОТО_URL | Ссылка на фото |
| L | Продавец_TG_ID | ID продавца |
| M | Статус | 🟢/🟡/🔴 |
| N | Описание | AI-описание |
| O | SEO_Title | SEO-заголовок |
| P | SEO_Description | SEO-описание |
| Q | Stock_Count | Остаток |
| R | Минимум_складчиков | Порог складчины |
| S | Складчина_участников | Участников |
| T | Цена_ориентир | Рыночная цена |
| U | Последний_интерес | Дата |

## 📦 Статический экспорт

Система генерирует JSON-файлы в `./static_export/`:

```
static_export/
├── catalog.json              # Полный каталог
└── products/
    ├── VIN-00001.json       # Индивидуальные карточки
    ├── VIN-00002.json
    └── ...
```

### Пример catalog.json

```json
{
  "generated_at": "2024-12-08T12:00:00",
  "total_records": 150,
  "records": [
    {
      "article_id": "VIN-00001",
      "title": "The Dark Side of the Moon",
      "artist": "Pink Floyd",
      "genre": "Progressive Rock",
      "year": 1973,
      "format": "LP",
      "condition": "Near Mint",
      "price": 5000,
      "stock_count": 1
    }
  ]
}
```

## 🔧 Архитектура

```
┌─────────────────────┐
│   Google Sheets     │  ← Единый источник правды (SSOT)
│   (Справочник)      │
└──────────┬──────────┘
           │
           │ gspread
           ▼
┌─────────────────────┐
│   SheetsClient      │  ← Бизнес-логика
│   (utils/)          │
└──────────┬──────────┘
           │
      ┌────┴────┐
      │         │
      ▼         ▼
┌──────────┐ ┌──────────┐
│ FastAPI  │ │ Telegram │
│ Backend  │ │   Bot    │
└────┬─────┘ └──────────┘
     │
     ├─── /api/records (JSON для фронтенда)
     └─── /admin/export-static (статические файлы)
```

## 🛠️ Технологический стек

- **Backend**: Python 3.11+, FastAPI, uvicorn
- **Database**: Google Sheets (через gspread)
- **Bot**: python-telegram-bot
- **Storage**: Google Drive (для фото)
- **AI**: OpenAI API (опционально)
- **Export**: JSON (статические файлы)

## 📚 Документация

- [SETUP.md](SETUP.md) — детальная инструкция по настройке
- [migrate_sheets.py](migrate_sheets.py) — скрипт миграции структуры

## 🔒 Безопасность

- ✅ Проверка ADMIN_TELEGRAM_ID для всех админских действий
- ✅ Валидация данных через Pydantic
- ⚠️ Для production: используйте HTTPS и API keys
- ⚠️ Ограничьте доступ к `/admin/*` эндпоинтам

## 🐛 Устранение неполадок

### Бот не отвечает

```bash
# Проверьте логи
journalctl -u vinyl-bot -f

# Убедитесь, что токен корректный
echo $TELEGRAM_BOT_TOKEN

# Проверьте, что нет конфликтов (другой процесс с тем же токеном)
ps aux | grep vinyl_bot
```

### API возвращает ошибки

```bash
# Проверьте доступ к Google Sheets
python -c "from utils.sheets_client import SheetsClient; print('OK')"

# Проверьте логи API
journalctl -u vinyl-api -f
```

### Не создаются артикулы

Убедитесь, что:
1. Первая строка содержит заголовки
2. Колонка A называется "Артикул"
3. Запустили `migrate_sheets.py`

## 📈 Масштабирование

- **>1000 записей**: Переход на PostgreSQL
- **Высокая нагрузка**: Redis для кеширования
- **Автообновление**: Cron для периодической генерации JSON
- **CDN**: Vercel/Cloudflare для статических файлов

## 🤝 Участие в разработке

1. Fork репозитория
2. Создайте feature-ветку
3. Commit изменения
4. Push в ветку
5. Создайте Pull Request

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Система разработана для управления каталогом виниловых пластинок с поддержкой:
- Telegram-бота для админов
- REST API для React/Vue фронтенда
- Статической генерации для Vercel/Netlify
- AI-описаний и SEO-оптимизации

---

**Версия**: 1.0.0  
**Дата**: Декабрь 2024
