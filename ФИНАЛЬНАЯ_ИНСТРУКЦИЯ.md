# ✅ ВСЁ ГОТОВО К ДЕПЛОЮ - ФИНАЛЬНАЯ ИНСТРУКЦИЯ

## 🎯 ЧТО Я СДЕЛАЛ:

### ✅ 1. Переименовал "Наши услуги" → "Услуги электрика"
**Файл:** `electric-service-automation-main/src/components/Landing.tsx`
```tsx
// Строка 91
<h2 className="text-4xl font-bold text-center text-gray-900 mb-12">Услуги электрика</h2>
```

### ✅ 2. Добавил систему скидок за объем
**Файл:** `electric-service-automation-main/src/components/products/initialContainers.ts`

Добавлены скидки для всех основных услуг:
- **Розетки/Выключатели:** 3+ шт → 5%, 5+ шт → 10%
- **Светильники:** 3+ шт → 5%, 5+ шт → 10%
- **И другие услуги**

Пример кода:
```typescript
{ 
  id: 'install', 
  name: 'Установить розетку', 
  price: 250, 
  quantity: 1, 
  enabled: false, 
  discount: { minQuantity: 3, percent: 5 } 
}
```

### ✅ 3. Улучшил кнопку "Редактировать" в корзине
**Файл:** `electric-service-automation-main/src/pages/Cart.tsx`

Добавлена кнопка "← К услугам" для возврата к выбору:
```tsx
<Button
  size="sm"
  variant="ghost"
  onClick={() => navigate('/products')}
  className="h-7 text-blue-600 hover:text-blue-700 hover:bg-blue-50 text-xs"
  title="Вернуться к выбору услуг"
>
  <Icon name="ArrowLeft" size={14} className="mr-1" />
  К услугам
</Button>
```

---

## ⚠️ ПРОБЛЕМА С ДЕПЛОЕМ:

На вашем Mac проблема с Node.js (несовместимость с macOS 12.7.6).

**Ошибка:**
```
dyld[14379]: Symbol not found: (__ZNSt3__122__libcpp_verbose_abortEPKcz)
```

---

## 🚀 РЕШЕНИЕ: СОБРАТЬ НА VPS

### Простой способ (рекомендую):

**1. Скопируйте проект на VPS:**

Откройте терминал и выполните:

```bash
cd /Users/user/Documents/Projects/Github/balt-set.ru
tar -czf electric-service-automation-main.tar.gz electric-service-automation-main/
scp electric-service-automation-main.tar.gz root@176.98.178.109:/tmp/
```

**Пароль:** `vfhufhbnrf`

**2. Подключитесь к VPS:**

```bash
ssh root@176.98.178.109
```

**Пароль:** `vfhufhbnrf`

**3. На VPS выполните:**

```bash
# Переходим в /tmp
cd /tmp

# Распаковываем проект
tar -xzf electric-service-automation-main.tar.gz

# Переходим в папку проекта
cd electric-service-automation-main

# Устанавливаем зависимости (если Node.js уже установлен на VPS)
npm install

# Собираем проект
npm run build

# Копируем собранные файлы в папку сайта
cp -r dist/* /var/www/app.balt-set.ru/

# Устанавливаем права
chmod -R 755 /var/www/app.balt-set.ru/

# Готово!
echo "✅ Деплой завершен! Проверьте: https://app.balt-set.ru/"
```

**4. Если Node.js НЕ установлен на VPS:**

```bash
# Установить Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# Проверить установку
node --version
npm --version

# Затем продолжить с пункта 3 (npm install и т.д.)
```

---

## 📋 АЛЬТЕРНАТИВА: ДЕПЛОЙ TELEGRAM FOLDERS

Пока решаете проблему с React-приложением, можно задеплоить готовую фичу Telegram Folders:

### Команды для копирования файлов:

```bash
cd /Users/user/Documents/Projects/Github/balt-set.ru

# Копируем файлы по очереди
scp telegram_folders_integration.py root@176.98.178.109:/root/ai_service_bots/
# Пароль: vfhufhbnrf

scp telegram_client_bot.py root@176.98.178.109:/root/ai_service_bots/
# Пароль: vfhufhbnrf
```

### Перезапуск бота на VPS:

```bash
ssh root@176.98.178.109
# Пароль: vfhufhbnrf

cd /root/ai_service_bots/

# Останавливаем старый процесс
pkill -f telegram_client_bot.py

# Ждем 2 секунды
sleep 2

# Запускаем новый процесс
nohup python3 telegram_client_bot.py > client_bot.log 2>&1 &

# Проверяем что запустился
ps aux | grep telegram_client_bot.py | grep -v grep

# Смотрим логи
tail -10 client_bot.log

# Выходим
exit
```

### Тестирование Telegram Folders:

1. Откройте @ai_service_client_bot в Telegram
2. Создайте новую заявку (любой адрес, телефон)
3. Подтвердите заявку
4. **Должна появиться кнопка:** `📁 Добавить папку "⚡ Электрик БАЛТСЕТЬ"`
5. Нажмите на кнопку
6. Telegram предложит создать папку со всеми чатами BALT-SET

---

## 🧪 КАК ПРОВЕРИТЬ ИЗМЕНЕНИЯ ПОСЛЕ ДЕПЛОЯ:

### Тест 1: Название секции
```
1. Откройте https://app.balt-set.ru/
2. Прокрутите до секции услуг
3. ✅ Должно быть "Услуги электрика" (было "Наши услуги")
```

### Тест 2: Скидки
```
1. На сайте перейдите в калькулятор
2. Выберите "Установить розетку"
3. Увеличьте количество до 3
4. ✅ Должно появиться: "Скидка 5% от 3 шт. ✓ Применена!" (зеленым)
5. ✅ Цена: 712.5 ₽ вместо 750 ₽
```

### Тест 3: Кнопка "К услугам"
```
1. Добавьте услуги в корзину
2. Перейдите в корзину
3. Нажмите "Редактировать" (справа вверху)
4. ✅ Появляется кнопка "← К услугам" 
5. Нажмите на неё
6. ✅ Вернулись к выбору услуг
```

---

## 📊 ЧТО ИЗМЕНИЛОСЬ (ВИЗУАЛЬНО):

### ДО:
```
Наши услуги
├── Установить розетку: 250 ₽
└── Количество: [1]
```

### ПОСЛЕ:
```
Услуги электрика
├── Установить розетку: 250 ₽
│   Скидка 5% от 3 шт. (серым)
└── Количество: [3]
    Скидка 5% от 3 шт. ✓ Применена! (зеленым, жирным)
    Цена: 712.5 ₽
```

---

## 📁 ФАЙЛЫ ГОТОВЫЕ К ДЕПЛОЮ:

```
balt-set.ru/
├── electric-service-automation-main/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Landing.tsx ✅ ИЗМЕНЕН (Услуги электрика)
│   │   │   └── products/
│   │   │       └── initialContainers.ts ✅ ИЗМЕНЕН (Скидки)
│   │   └── pages/
│   │       └── Cart.tsx ✅ ИЗМЕНЕН (Кнопка "К услугам")
│   └── package.json
├── telegram_folders_integration.py ✅ ГОТОВ
├── telegram_client_bot.py ✅ ИЗМЕНЕН (Интеграция folders)
└── ДОКУМЕНТАЦИЯ:
    ├── README_ДЕПЛОЙ.md ← Начните здесь
    ├── ДЕПЛОЙ_УЛУЧШЕНИЙ.md
    ├── УЛУЧШЕНИЯ_СИСТЕМЫ_РЕАЛИЗОВАНО.md
    ├── TELEGRAM_FOLDERS_GUIDE.md
    └── ДЕПЛОЙ_СТАТУС.md
```

---

## 💡 СОВЕТЫ:

### Если нужна помощь с Node.js на VPS:
```bash
# Узнать версию Node.js на VPS
ssh root@176.98.178.109 "node --version"

# Если Node.js не установлен
ssh root@176.98.178.109 "curl -fsSL https://deb.nodesource.com/setup_18.x | bash - && apt-get install -y nodejs"
```

### Если нужно очистить кеш браузера:
```
Chrome: Ctrl+Shift+Delete
Firefox: Ctrl+Shift+Delete
Safari: Cmd+Option+E

Или добавьте ?v=3 к URL:
https://app.balt-set.ru/?v=3
```

### Если файлы не отображаются:
```bash
# Проверить права доступа
ssh root@176.98.178.109 "ls -la /var/www/app.balt-set.ru/"

# Установить правильные права
ssh root@176.98.178.109 "chmod -R 755 /var/www/app.balt-set.ru/"
```

---

## 🎯 ИТОГ:

### Все изменения сделаны ✅
### Файлы готовы к деплою ✅
### Документация создана ✅

### Осталось только:
1. Скопировать проект на VPS (команда выше)
2. Собрать на VPS (npm run build)
3. Скопировать в папку сайта (cp -r dist/*)
4. Проверить сайт (https://app.balt-set.ru/)

---

## 📞 БЫСТРАЯ ПОМОЩЬ:

**Вся команда одной строкой (выполнить на VPS):**

```bash
cd /tmp && tar -xzf electric-service-automation-main.tar.gz && cd electric-service-automation-main && npm install && npm run build && cp -r dist/* /var/www/app.balt-set.ru/ && chmod -R 755 /var/www/app.balt-set.ru/ && echo "✅ Готово! https://app.balt-set.ru/"
```

Только сначала скопируйте архив на VPS (команда из раздела "РЕШЕНИЕ").

---

🚀 **Готов помочь если что-то не получится!**
