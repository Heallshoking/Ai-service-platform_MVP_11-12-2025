# ⚡ Быстрая настройка Telegram для калькулятора

## Шаг 1: Создайте Telegram бота (5 минут)

### 1.1 Откройте @BotFather
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Нажмите "Начать" или `/start`

### 1.2 Создайте нового бота
```
Отправьте: /newbot

BotFather спросит:
> Alright, a new bot. How are we going to call it?

Ответьте: БАЛТСЕТЬ Калькулятор

BotFather спросит:
> Good. Now let's choose a username for your bot.

Ответьте: baltset_calculator_bot
(или любое имя, заканчивающееся на _bot)
```

### 1.3 Сохраните токен
```
BotFather ответит:
> Done! Congratulations on your new bot.
> Use this token to access the HTTP API:
> 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

⚠️ СКОПИРУЙТЕ И СОХРАНИТЕ ЭТОТ ТОКЕН!
```

---

## Шаг 2: Создайте группу для уведомлений (2 минуты)

### 2.1 Создайте группу
1. В Telegram создайте новую группу
2. Назовите её "БАЛТСЕТЬ - Заявки"
3. Добавьте вашего бота в группу (найдите по username)

### 2.2 Дайте боту права админа
1. Откройте настройки группы
2. Администраторы → Добавить администратора
3. Выберите вашего бота
4. Дайте право "Отправлять сообщения"

---

## Шаг 3: Получите Chat ID группы (1 минута)

### Вариант A: Через API
1. Отправьте любое сообщение в группу
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
3. Найдите в ответе:
   ```json
   "chat": {
     "id": -1001234567890,  ← Это ваш chat_id!
     "title": "БАЛТСЕТЬ - Заявки"
   }
   ```

### Вариант B: Через бота
1. Добавьте [@userinfobot](https://t.me/userinfobot) в группу
2. Бот автоматически покажет ID группы
3. Удалите @userinfobot из группы

---

## Шаг 4: Обновите калькулятор (30 секунд)

### Откройте файл калькулятора
```bash
nano /Users/user/Documents/Projects/Github/balt-set.ru/static/calculator-integrated.html
```

### Найдите строки 755-756 и замените:
```javascript
// БЫЛО:
const TELEGRAM_BOT_TOKEN = 'YOUR_BOT_TOKEN';
const TELEGRAM_CHAT_ID = 'YOUR_CHAT_ID';

// СТАЛО:
const TELEGRAM_BOT_TOKEN = '1234567890:ABCdefGHIjklMNOpqrsTUVwxyz'; // ← Вставьте ваш токен
const TELEGRAM_CHAT_ID = '-1001234567890'; // ← Вставьте ваш chat_id
```

### Найдите строку 893 и замените:
```javascript
// БЫЛО:
const botUsername = 'konigelectric';

// СТАЛО:
const botUsername = 'baltset_calculator_bot'; // ← Ваш username бота
```

### Сохраните файл
```bash
Ctrl+O → Enter → Ctrl+X
```

---

## Шаг 5: Перезапустите сервер (10 секунд)

```bash
# Остановите текущий сервер
pkill -f "uvicorn main:app"

# Запустите заново
cd /Users/user/Documents/Projects/Github/balt-set.ru
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

---

## Шаг 6: Тестирование (2 минуты)

### 6.1 Откройте калькулятор
```
http://localhost:8000/calculator-integrated
```

### 6.2 Протестируйте чат
1. Введите в чат: "Сколько стоит установка розетки?"
2. Нажмите кнопку отправки 📤
3. **Проверьте Telegram группу** - должно прийти сообщение:
   ```
   💬 Новый вопрос от клиента:
   
   Сколько стоит установка розетки?
   
   ——————————————
   🕗 14.12.2025, 23:50
   ```

### 6.3 Протестируйте lead magnet
1. Выберите "2-комнатная квартира"
2. Нажмите "Рассчитать стоимость"
3. Должно открыться модальное окно
4. Введите номер телефона: +7 (912) 345-67-89
5. **Проверьте Telegram группу** - должно прийти:
   ```
   📞 НОВЫЙ ЛИД!
   
   👤 Телефон: +7 (912) 345-67-89
   🏠 Помещение: 🏡 2-комнатная
   📊 Источник: Подтверждение для просмотра сметы
   🕗 14.12.2025, 23:50
   ```

### 6.4 Протестируйте смету
1. После разблокировки измените количество услуг
2. Нажмите "Получить смету на телефон"
3. **Проверьте Telegram группу** - должна прийти детальная смета

---

## ✅ Готово!

Теперь ваш калькулятор полностью интегрирован с Telegram и будет автоматически отправлять вам:
- ❓ Вопросы клиентов из чата
- 📞 Новые лиды с телефонами
- 📄 Детальные сметы

---

## 🚀 Деплой на продакшн

### Когда всё протестировано локально:

```bash
# 1. Закоммитьте изменения
git add static/calculator-integrated.html
git commit -m "feat: настроен Telegram для калькулятора"
git push origin main

# 2. Timeweb автоматически задеплоит

# 3. Через 2-3 минуты проверьте:
https://app.balt-set.ru/calculator-integrated
```

---

## 📱 Бонус: Настройка кнопки "Отправить контакт"

Чтобы в боте работала кнопка "Отправить номер телефона":

### Создайте файл bot.py
```python
from telegram import ReplyKeyboardMarkup, KeyboardButton
from telegram.ext import Updater, CommandHandler, MessageHandler, Filters

TOKEN = "ВАШ_ТОКЕН"

def start(update, context):
    # Кнопка для отправки контакта
    button = KeyboardButton("📱 Отправить номер телефона", request_contact=True)
    keyboard = ReplyKeyboardMarkup([[button]], resize_keyboard=True)
    
    update.message.reply_text(
        "👋 Привет! Для получения сметы поделитесь своим номером телефона:",
        reply_markup=keyboard
    )

def contact_received(update, context):
    contact = update.message.contact
    phone = contact.phone_number
    
    update.message.reply_text(
        f"✅ Спасибо! Ваш номер {phone} получен.\n"
        f"Смета будет отправлена в течение 60 секунд!"
    )
    
    # Здесь можно сохранить контакт в БД

updater = Updater(TOKEN)
updater.dispatcher.add_handler(CommandHandler('start', start))
updater.dispatcher.add_handler(MessageHandler(Filters.contact, contact_received))
updater.start_polling()
updater.idle()
```

### Запустите бота
```bash
pip install python-telegram-bot
python bot.py
```

---

**Время настройки:** ~10 минут  
**Результат:** Автоматический сбор лидов 24/7  
**ROI:** Бесценно! 🚀
