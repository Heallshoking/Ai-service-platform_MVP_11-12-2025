# 🚀 АВТОМАТИЧЕСКИЙ ДЕПЛОЙ - СТАТУС

## ⚠️ ПРОБЛЕМА С NODE.JS

При попытке собрать проект возникла ошибка:
```
dyld[14379]: Symbol not found: (__ZNSt3__122__libcpp_verbose_abortEPKcz)
Abort trap: 6
```

Это проблема совместимости Node.js на вашей системе macOS 12.7.6.

---

## ✅ ЧТО УЖЕ ГОТОВО:

Все файлы изменены и готовы к деплою:

1. ✅ **Landing.tsx** - "Услуги электрика" вместо "Наши услуги"
2. ✅ **initialContainers.ts** - Система скидок за объем добавлена
3. ✅ **Cart.tsx** - Кнопка "← К услугам" добавлена

---

## 🔧 РЕШЕНИЕ:

### Вариант 1: Собрать на VPS (РЕКОМЕНДУЕТСЯ)

```bash
# Подключиться к VPS
ssh root@176.98.178.109

# Установить Node.js если нет
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# Клонировать репозиторий или скопировать файлы
cd /var/www/
git clone [your-repo] app-source
# ИЛИ скопировать папку electric-service-automation-main

cd app-source/electric-service-automation-main
npm install
npm run build

# Скопировать собранные файлы
cp -r dist/* /var/www/app.balt-set.ru/
```

### Вариант 2: Использовать готовую статическую версию

Обновить существующий calculator-integrated.html который уже работает:

```bash
# Деплой обновленного статического калькулятора
scp static/calculator-integrated.html root@176.98.178.109:/var/www/bag4moms/data/www/bag4moms.balt-set.ru/calculator.html
```

### Вариант 3: Исправить Node.js локально

```bash
# Удалить текущий Node.js
brew uninstall node

# Установить правильную версию
brew install node@18

# Добавить в PATH
echo 'export PATH="/usr/local/opt/node@18/bin:$PATH"' >> ~/.bash_profile
source ~/.bash_profile

# Проверить
node --version

# Собрать проект
cd electric-service-automation-main
npm install
npm run build
```

---

## 📋 ЧТО ДЕЛАТЬ СЕЙЧАС:

### Если хочешь быстро задеплоить изменения:

**Выполни на VPS:**

```bash
ssh root@176.98.178.109

# Установить Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs

# Создать рабочую папку
mkdir -p /var/www/app-build
cd /var/www/app-build
```

**Потом на локальном компьютере:**

```bash
# Скопировать весь проект на VPS
cd /Users/user/Documents/Projects/Github/balt-set.ru
scp -r electric-service-automation-main root@176.98.178.109:/var/www/app-build/
```

**Снова на VPS:**

```bash
cd /var/www/app-build/electric-service-automation-main
npm install
npm run build
cp -r dist/* /var/www/app.balt-set.ru/
chmod -R 755 /var/www/app.balt-set.ru/
```

---

## 🎯 АЛЬТЕРНАТИВА: ДЕПЛОЙ ТОЛЬКО TELEGRAM FOLDERS

Пока проблема с React-проектом решается, можно задеплоить уже готовые фичи:

### 1. Telegram Folders Integration

```bash
scp telegram_folders_integration.py root@176.98.178.109:/root/ai_service_bots/
scp telegram_client_bot.py root@176.98.178.109:/root/ai_service_bots/

ssh root@176.98.178.109
cd /root/ai_service_bots/
pkill -f telegram_client_bot.py
sleep 2
nohup python3 telegram_client_bot.py > client_bot.log 2>&1 &
```

### 2. Google Sheets Integration

Следовать инструкции в `БЫСТРЫЙ_СТАРТ_GOOGLE_SHEETS.md`

---

## 💡 РЕКОМЕНДАЦИЯ:

**Самый простой путь:**

1. Собрать проект на VPS (там Node.js работает нормально)
2. Скопировать готовую сборку в нужную папку

**Команды:**

```bash
# 1. Скопировать проект на VPS
scp -r electric-service-automation-main root@176.98.178.109:/tmp/

# 2. Подключиться к VPS  
ssh root@176.98.178.109

# 3. Собрать на VPS
cd /tmp/electric-service-automation-main
npm install
npm run build

# 4. Задеплоить
cp -r dist/* /var/www/app.balt-set.ru/
chmod -R 755 /var/www/app.balt-set.ru/

# 5. Проверить
echo "Готово! https://app.balt-set.ru/"
```

---

## 📞 НУЖНА ПОМОЩЬ?

Выбери один из вариантов:

1. **Собрать на VPS** (рекомендуется) - нужен доступ к VPS
2. **Исправить Node.js локально** - займет время
3. **Деплой Telegram Folders** - работает уже сейчас

Какой вариант выбираешь?
